#include <stdio.h>
#include <stdlib.h>

int main() {
    char s1[] = "999", s2[] = "999";
    
    int n1 = atoi(s1);
    int n2 = atoi(s2);

    int sum = n1 + n2;
    printf("sum: %d", sum);

    return 0;
}
